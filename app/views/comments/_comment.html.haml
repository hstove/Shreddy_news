%table
  %tr.comment
    %td
      %span.content= @comment.content
      %br
      %span.posted= time_ago_in_words(@comment.created_at) + " ago"
      -if @comment.user_id?
        %td= User.find(@comment.user_id).username