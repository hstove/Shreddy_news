%h1 Admin#index
%p Find me in app/views/admin/index.html.haml

%table
  %colgroup.title
  %colgroup.edit
  - @posts.each do |post|
    %tr
      %td= link_to post.title + '(' + post.id.to_s + ')', "http://#{post.url}"
      %td= link_to 'edit', edit_post_path(post)
      %td= link_to 'delete', post, :method => :delete
  