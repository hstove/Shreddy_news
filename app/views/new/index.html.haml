#box= render @post
#postbox= render :partial => 'posts'

-if user_signed_in?      
  .newpost= link_to 'New Post', new_user_post_path(:user_id => @user.id), :class => "btn success"
  
= will_paginate @posts
